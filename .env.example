# ===========================================
# AI Prompt Web - 환경변수 템플릿
# ===========================================
# 이 파일을 복사하여 .env.production 또는 .env 로 저장하세요
# cp .env.example .env.production

# ===========================================
# OpenAI API
# ===========================================
OPENAI_API_KEY=sk-your-openai-api-key-here

# ===========================================
# Tavily Search API (선택사항 - 웹 검색 기능용)
# ===========================================
TAVILY_API_KEY=tvly-your-tavily-api-key-here

# ===========================================
# 데이터베이스 설정
# ===========================================
DB_USER=postgres
DB_PASSWORD=your-secure-database-password
DB_NAME=ai_prompt_db

# ===========================================
# 보안 설정
# ===========================================
# 시크릿 키 생성: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-generate-new-one

# ===========================================
# Cloudflare Tunnel
# ===========================================
# Cloudflare Zero Trust에서 Tunnel 생성 후 토큰 복사
# https://one.dash.cloudflare.com > Networks > Tunnels
CLOUDFLARE_TUNNEL_TOKEN=your-cloudflare-tunnel-token

# ===========================================
# CORS 설정 (코드에서 기본값 사용)
# ===========================================
# CORS는 backend/app/core/config.py에서 설정됨
# 기본값: localhost, shoneylife.com 등

# ===========================================
# 프론트엔드 설정
# ===========================================
VITE_API_BASE_URL=https://shoneylife.com/api/v1